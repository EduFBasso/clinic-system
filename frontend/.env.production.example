# frontend\.env.production.example
# Use when building locally for production (or in CI without Vercel).
# In Vercel, set this in Project Settings → Environment Variables
# and scope it to Production (and Preview for staging if needed).
# Example:
# VITE_API_BASE=https://your-backend.onrender.com
VITE_API_BASE=
