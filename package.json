{
  "name": "clinic-system-monorepo",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run -w frontend build",
    "test": "npm run -w frontend test",
    "lint": "npm run -w frontend lint",
    "dev:frontend": "npm run -w frontend dev",
    "preview": "npm run -w frontend preview"
  }
}
